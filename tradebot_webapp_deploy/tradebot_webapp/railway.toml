[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn webapp.app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
